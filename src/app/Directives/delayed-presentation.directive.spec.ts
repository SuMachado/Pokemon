import { DelayedPresentationDirective } from './delayed-presentation.directive';

describe('DelayedPresentationDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayedPresentationDirective();
    expect(directive).toBeTruthy();
  });
});
